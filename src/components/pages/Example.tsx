import { colors, breakpoints, hugeFontSize } from "../../theme/variables";
import Button from "../atoms/Button";

const Example = () => {
  return (
    <>
      <div>Example components for Startup Boilerplate</div>
      <Button type="submit">Send!</Button>
    </>
  );
};

export default Example;
